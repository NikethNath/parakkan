# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env & secrets — NEVER commit these
.env
.env.local
.env.*.local
*.pem

# private data — the dealer's real bank/CRIS files (NEVER commit)
*.xls
*.xlsx
*.csv
Daily_Sales_Report*
178229004953986*

# misc
.DS_Store
*.log
npm-debug.log*

# testing
/coverage

# prisma local db data (if using a local volume)
/prisma/data

# database backups (created on the server)
/backups

# editor
.vscode/*
!.vscode/extensions.json
.idea
