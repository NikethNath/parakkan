# Production stack: app + PostgreSQL + Caddy (automatic HTTPS).
# Configure via a .env file next to this one (see .env.production.example).

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: hpcl
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: hpcl
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hpcl"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    restart: unless-stopped
    init: true
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://hpcl:${POSTGRES_PASSWORD}@db:5432/hpcl?schema=public
      SESSION_SECRET: ${SESSION_SECRET}
      SECRET_KEY: ${SECRET_KEY}
      CRON_SECRET: ${CRON_SECRET}
      ADMIN_USERNAME: ${ADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      CHROME_PATH: /usr/bin/chromium
    expose:
      - "3000"

  # Nightly CRIS auto-fetch. busybox crond fires cris-fetch.sh at 03:00 IST,
  # which POSTs the token-protected /api/cris/cron-fetch endpoint on the app
  # (reached directly over the internal network). Logs to `docker compose logs
  # cron`. No-ops with a logged warning if CRON_SECRET is unset.
  cron:
    image: alpine:3.20
    restart: unless-stopped
    depends_on:
      - app
    environment:
      TZ: Asia/Kolkata
      CRON_SECRET: ${CRON_SECRET}
      CRIS_CRON_URL: http://app:3000/api/cris/cron-fetch
      CRIS_LOG: /proc/1/fd/1
    volumes:
      - ./scripts:/scripts:ro
    command:
      - /bin/sh
      - -c
      - |
        set -e
        apk add --no-cache curl tzdata >/dev/null
        mkdir -p /etc/crontabs
        printf '0 3 * * * /scripts/cris-fetch.sh\n' > /etc/crontabs/root
        echo "[cron] CRIS auto-fetch scheduled for 03:00 Asia/Kolkata"
        exec crond -f -l 8 -c /etc/crontabs

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  pgdata:
  caddy_data:
  caddy_config:
