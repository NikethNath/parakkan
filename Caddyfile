# Caddy auto-provisions and renews a Let's Encrypt certificate for $DOMAIN.
# Requires: $DOMAIN's DNS A record points to this server, ports 80 + 443 open.
{$DOMAIN} {
	encode gzip
	reverse_proxy app:3000
}
